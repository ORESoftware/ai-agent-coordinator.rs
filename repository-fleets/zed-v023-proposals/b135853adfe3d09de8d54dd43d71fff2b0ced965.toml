[package]
org = "fiducia-cloud-test"
name = "python-client-consumer"
version = "0.1.0"
description = "Independent consumer acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/fiducia-cloud-test/python-client-consumer"

[install]
dir = ".vendor/.zed"

[dependencies]
"fiducia-cloud/fiducia-clients-python" = "*"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
