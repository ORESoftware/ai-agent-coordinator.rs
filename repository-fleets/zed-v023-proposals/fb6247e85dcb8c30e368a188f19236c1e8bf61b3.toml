[package]
org = "fiducia-cloud-test"
name = "rust-client-consumer"
version = "0.1.0"
description = "Independent consumer acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/fiducia-cloud-test/rust-client-consumer"

[install]
dir = ".vendor/.zed"

[dependencies]
"fiducia-cloud/fiducia-clients-rust" = "*"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
